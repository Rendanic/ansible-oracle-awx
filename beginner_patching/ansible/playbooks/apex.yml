---
- name: APEX
  hosts: "{{ hostgroup | default('all') }}"
  any_errors_fatal: true
  tasks:
    - name: APEX
      ansible.builtin.import_role:
        name: opitzconsulting.ansible_oracle.oraapex
